import React from 'react'
import './SearchButton.css'

const SearchButton = () =>{

    return (
        <section className='containerBSP'>
            <button className='searchPlacesBtn'>Search for places</button>
        </section>
    );
}

export default SearchButton;